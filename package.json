{
  "name": "temp-man",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=dev && jest --watch",
    "dev": "nodemon -w src/ -e ts -x \"tsc && ./bin/cli\"",
    "once": "tsc && ./bin/cli component ComponentName  ./src"
  },
  "bin": {
    "generator": "./bin/cli"
  },
  "jest": {
    "preset": "ts-jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.12.17",
    "eventemitter3": "^4.0.0",
    "jest": "^24.9.0",
    "typescript": "^3.7.3"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "ts-jest": "^24.2.0"
  }
}
